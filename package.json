{
  "name": "quran-video-maker",
  "version": "1.0.0",
  "author": "QuranVideoMaker",
  "description": "Lecteur Quran local dans Electron",
  "main": "electron/main.js",
  "scripts": {
    "start": "electron .",
    "build": "node scripts/build-desktop.js",
    "build:installer": "node scripts/build-desktop.js --installer-only",
    "build:portable": "node scripts/build-desktop.js --portable-only",
    "build:web": "node scripts/build-web.js"
  },
  "dependencies": {
    "quran-search-engine": "^0.3.2"
  },
  "devDependencies": {
    "electron": "^29.0.0",
    "electron-builder": "^24.0.0",
    "rcedit": "^5.0.2"
  },
  "build": {
    "appId": "com.quranvideomaker.desktop",
    "productName": "QuranVideoMaker",
    "directories": {
      "output": "dist"
    },
    "win": {
      "target": [
        "nsis",
        "portable"
      ],
      "icon": "logo-build.ico",
      "signAndEditExecutable": false
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "runAfterFinish": false,
      "include": "build-installer.nsh",
      "installerIcon": "logo-build.ico",
      "uninstallerIcon": "logo-build.ico",
      "installerHeaderIcon": "logo-build.ico",
      "artifactName": "${productName}-Setup-${version}.${ext}"
    },
    "portable": {
      "artifactName": "${productName}-Portable-${version}.${ext}"
    },
    "files": [
      "electron/**",
      "index.html",
      "sw.js",
      "logo-build.ico",
      "logo-build.png",
      "logo.png",
      "manifest.webmanifest",
      "icons/**",
      "qul/**",
      "local_audio/**",
      "online/**"
    ],
    "extraResources": [
      {
        "from": ".build/runtime",
        "to": "runtime",
        "filter": [
          "**/*"
        ]
      }
    ]
  }
}
